# Loop Review Log（reviewer 判"已放弃·需人工"的卡）

> reviewer 自动质检时，对**重写 2 次仍不达标**的卡标记 `loop·已优化·已放弃` 并 append 到这里。人工有空时挑着改，改好后把 hint 里的 `loop·已优化·已放弃` 删掉（变回未标记），worker 下一轮会自动重做。

| 日期 | 卡名 | 原因 |
|---|---|---|
