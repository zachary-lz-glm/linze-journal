# 明日面试 · 预测题校准与匹配分析（战斗手册）

> 校准依据：交叉核对了四份真实资料——① 你的诚实边界档案 `00-个人面试档案.md`；② `Manager_Agent` 源码；③ `code_assistent_Agent` 源码；④ `prd-tools` 源码（v2.20.1）。
> 标记法：✅ 代码可举证、放心说 ｜ ⚠️ 需软化/限定 ｜ ❌ 零证据或已被反证、**绝对别说**。

---

## 0. ⚠️ 务必先读：预测答案里的「雷」

你贴的预测答案 + 诚实边界档案，有多处数字/技术声明在真实代码里立不住，被深挖必穿帮。**最大隐患不是能力，是表述**。最致命的几条：

| ❌ 预测答案原话 | 真相 | 处理 |
|---|---|---|
| 「把幻觉导致的错误计划率从 40% 降到 15% 以下」 | prd-tools 仓库零数据，benchmarks/results 全空 | **删** |
| 「端到端原型 prd2code-gen，6 步闭环」 | 该目录/项目不存在 | **删** |
| 「已推广 20+ 人团队 / 生产在用」 | ADR-0005 明写团队 1-5 人，无推广记录 | **改口** |
| 「五维 quality-gate：需求覆盖率/代码质量/可维护性/安全性/可追溯性」 | 维度名全错 | **改口**（见 §3） |
| 「代码结构走 AST、编码规则走 ESLint」 | 实为 rg+Read+正则；AST 在 v2.13 已删；ESLint 零命中 | **改口** |
| 「Claude 3 Opus 做 Coding Agent 核心模型 / GPT-4 做 Supervisor」 | 你 Agent 项目实际跑阿里云 qwen；只有 prd-tools 用 Claude | **改口** |
| 「配置时间 2h→15min / 错误率 20%→5% / 点击率 +15% / 2.8s→1.2s / 5 条业务线」 | 编造的实测数据，诚实边界档案明令禁止 | **删** |

下面逐题给出「安全版答案」。**优先记 ❌ 红线和 ✅ 可说的，别背原预测答案。**

---

## 1. 匹配分析（校准版）

**岗位画像**：Agent 工程化方向 + 产品 Leader 视角，创业团队，重业务落地与协作。

**真实优势（代码可举证，是真材实料不是 PPT）**：
1. **全链路 Agent 实战**：编排层（Manager 39 节点状态机）+ Coding Agent（code_assistant 18 工具）+ 安全（四层写盘约束）+ 自进化（shadow→金丝雀→A/B），一条龙都有真实代码。
2. **工程完成度高**：A/B 进化非劣性设计（lift +0.03 晋级 / −0.06 回滚）、evidenceGate 无引用不 final、sha256 乐观并发锁、JWT/scope 鉴权、retryBudget 分层治死循环——都是可深聊的硬实现。
3. **前端 + AI 复合**：Schema 驱动渲染引擎（依赖图+拓扑排序+循环检测）+ AI 工程化，能讲清「从业务抽共性做架构」。
4. **防幻觉是主线**：prd-tools 证据链（每条挂 EV-XXX ID）+ Manager evidenceGate + code_assistant read-before-write，理念一致、可串联。

**真实短板（要主动补位）**：
1. **创业适配证据弱**：4 年大厂，需用**周末个人项目**（prd-tools 已迭代到 v2.20.1、两个 Agent 项目）证明激情与快速试错——这是你最强的创业适配证据，Q1 要打这张牌。
2. **目标公司业务理解**：临场花 20 分钟体验对方产品，准备 1-2 条针对性优化点（见 §4 Q7）。
3. **prd-tools 包装与代码有 gap**：「平台/团队规模/端到端生成」是包装，代码现实是「Claude Code 插件 + 上下文编译器 + 证据门禁」。深挖 prd-tools 时务必用真实定位讲（见 §3）。
4. **性能基线缺失**：Schema 引擎缺三档性能对比，这是公认短板，别硬编实测（诚实边界已标）。

---

## 2. 数字口径红黑榜（背这一张就够）

### ✅ 可放心说（代码/数据可举证）

**Manager_Agent（13106，多智能体编排总管）**
- 39 节点 LangGraph 状态机（`managerGraph.graph.ts`，`addNode` 计数 = 39），7 组条件边谓词驱动所有旁路与回环
- Bandit(Thompson 采样) + RL(Q-learning 上下文桶) + Causal(ATT 近似因果) **三套自适应路由**，职责正交、互不冲突，都只产 routerHintBlock 提示、LLM 最终决策
- 自我进化：shadow→金丝雀（sessionId sha256 分桶，默认 5%）→ A/B（每臂 ≥8 样本，lift ≥+0.03 晋级 / ≤−0.06 回滚，非劣性设计）
- HITL 两套：plan_preview（进程内阻塞 600s）+ admin 写（跨进程 checkpoint，24h TTL，新 run resume）
- retryBudget + recursionLimit(48) 分层治 fix→synth 死循环
- evidenceGate：联网/查数任务无来源 → 不出 final
- 失败归因：规则式，驱动**离线 planner 硬规则进化**（越用越稳）

**code_assistant（13103，工程化 Coding Agent）**
- 18 个仓库工具（只读模式收敛到 11 个）
- LangGraph ToolNode + OpenAI function calling 循环（等价 ReAct 的 think-action-observation 语义）
- 自研向量检索（embedding = text-embedding-v3，内存余弦相似度）+ sha256 内容指纹惰性增量
- 安全（这是真亮点，逐条可举证）：JWT(jose) + scope 鉴权；IP 限流 60/min；路径白名单 + 敏感文件拦截（.env / id_rsa / .pem / .key）；sha256 乐观并发锁（冲突抛 409）；subprocess/docker 沙箱 + 超时；原子写盘（tmp+rename）；审计日志；chat-only 只读降级；写后自动 lint/typecheck/test + 自修复
- AST 分析用 `@typescript-eslint/typescript-estree`

**prd-tools（Claude Code 插件：PRD-to-Code 上下文编译器 + 证据门禁）**
- 两个 skill：`/reference`（建项目知识库）+ `/prd-distill`（PRD 蒸馏成 spec.md/plan.md + 证据中间产物）
- 证据链防幻觉：每条 requirement/impact/contract 挂 `EV-XXX` / `GCTX-XXX` ID
- Spec Review Gate：**代码级硬门禁**（`quality-gate.py` 校验 `spec-confirmation.yaml status: approved`，不通过 exit 2）
- Reference 六文件 SSOT 结构（codebase/coding-rules/contracts/routing-playbooks/domain/project-profile）
- 确定性 quality-gate 五维打分（见下），≥85 pass / ≥60 warning / <60 fail
- benchmark oracle 框架（5 维评分：requirement_recall / code_anchor_accuracy / blocker_quality / plan_actionability / false_positive_penalty，6 个真实 case，单 case 97 分）
- 模型 = **Claude**（通过 Claude Code 运行时）

**滴滴 Schema 驱动营销中台（主项目）**
- 20+ 活动类型；新增活动上线 **2 周→几小时**；一套引擎服务表单+权益 SDK 两个业务，**维护成本降一半**
- 核心机制：injectDActions 双重求值；联动本质是字段有向图依赖（依赖图建模 + 拓扑排序 + 循环检测）

**滴滴 权益组件库 benefit SDK**
- **30+ 组件**靠配置组合覆盖国际化多国家多业务线（**别说 400+**）
- Register 注册模式；Rollup 双模(ESM+CJS+d.ts) + peerDependencies + Changesets + yalc

**模型栈真相（Q2 必用）**
- prd-tools → Claude（Claude Code 运行时）
- Manager / code_assistant → 阿里云 DashScope 的 **qwen3.5-flash / qwen-coder-plus**（OpenAI 兼容接口）
- 这是「部署用便宜的 qwen、研发工作流用 Claude」的真实组合，讲出来反而显工程取舍

### ⚠️ 需软化/限定

- 节点数：代码 39，03 文档写 38 → 统一说「接近 40 个节点」最稳
- 失败归因「12 类」：类型表 12，运行时 10 类 + success（unclear 不可达）；**纯规则、非 LLM**；驱动离线规则进化、不参与当次重试 → 说「10–12 类规则式归因」
- 「每 30 分钟最多 3 条假设」：30min 节流真，但代码硬上限是 **4**（prompt 文本写 3）→ 说「每 30 分钟一批（上限 4 条）」
- 「命中指标自动晋级」：**自动晋级默认关闭**，走 ops 人工确认 → 说「指标门控 A/B + 默认人工确认晋级」
- Checkpoint「Custom CheckpointSaver 支持跨进程续跑」：**拆开讲，别连用**。自建快照是 16 字段业务子集（非 LangGraph state），file/redis/dual 三后端；dual 模式 Redis 失败静默不回退、无锁；真正的 LangGraph saver 是 Postgres/Memory、默认关
- code_assistant「Citation Grounding 每条事实附 file:line」：检索工具返回带行号，但**回答层无引用完整性硬校验**；「禁止推断/必须先读」是 prompt 软约束
- prd-tools「LLM 必须先读源码（硬门禁）」→ 降为「prompt 软约束 + evidence 字段事后门禁」
- prd-tools「60% 准确率」→ 改「readiness score 60–84 区间判 warning 并标 needs_owner_confirmation（需 owner 确认，非直接失败）」

### ❌ 绝对别说（零证据 / 被反证 / 诚实边界已禁）

- 「错误计划率 40%→15%」「配置 2h→15min」「错误率 20%→5%」「点击率 +15%」「2.8s→1.2s」「5 条业务线」——全是编造实测，删
- 「400+ 组件 / 18 个月零线上问题」——诚实边界已禁（无代码级证据）
- 「PRD→Plan 5h→2h」——诚实档案有，但**代码查无任何量化记录**，高风险。要么不提，要么标「体感估算」
- 「prd2code-gen 端到端原型 / 6 步闭环 / 双路径生成+自验证自修复」——prd-tools 不生成代码，该原型不存在
- 「已推广 20+ 人团队 / 生产在用」——ADR-0005 反证 1-5 人
- 「五维 = 需求覆盖率/代码质量/可维护性/安全性/可追溯性」——维度名全错
- 「代码结构走 AST、编码规则走 ESLint」——全错
- 「独立 agent 终检（第二个 LLM）」——不存在（废弃 ADR-0011 规划），终检是确定性 Python 脚本
- 「缓存 cache write 1.25× / read 0.1× / 命中省 90% / 4 个断点」——这是 Anthropic prompt caching 公开定价，**不是本项目工程**（项目无缓存代码），别挂自己头上
- 「Claude 3 Opus 做 Coding Agent 核心模型 / GPT-4 做 Supervisor」——你 Agent 项目跑的是 qwen；只有 prd-tools 用 Claude
- code_assistant「Diff 预览用户确认才执行 / 代码审查 Agent / HITL 高风险确认」——代码里没有
- 「Supervisor 模块」——代码零命中该词，叫「总管 / Router」；说「Supervisor **式**」可以

---

## 3. prd-tools 真实五维 quality-gate（必背，原答案全错）

`quality-gate.py` 真实五维（带权重）：

| 维度 | 权重 | 查什么 |
|---|---|---|
| `required_files` | 0.20 | 六个 reference 文件是否齐全 |
| `context_pack_consumed` | 0.15 | 计划是否真正消费了上下文包 |
| `code_anchor_coverage` | 0.25 | plan 是否覆盖关键代码锚点 |
| `plan_completeness` | 0.25 | 计划完整性 |
| `blocker_quality` | 0.15 | 阻塞项是否带 owner/缓解/风险六要素 |

另有独立的 **PRD coverage gate** 做 block / media / requirement trace 覆盖率（这才是最接近「需求覆盖率」的东西）。综合分 ≥85 pass / ≥60 warning / <60 fail。

> **没有任何「安全性」「可维护性」维度，项目里也没有安全扫描。被问到五维，按这张表答。**

---

## 4. 八道预测题 · 逐题校准

### Q1｜行为｜创业适配
> 大厂节奏稳定，能适配创业高强度快速迭代吗？举例说明激情和主动性。

❌ 别说：「牵头做 SDD Platform POC，一周原型、拉 20+ 人试用、1 个月落地」——20+ 人与 ADR 反证冲突，POC 数字无据。
✅ 安全版：用**真实的周末个人项目**当证据（这才是最强的创业适配证明）。

**可背答案**：
> 「我在滴滴后期 All-in LLM 时，已经在用创业式的节奏工作——没有现成 LLM 落地经验，我自己下班和周末持续打磨。比如 prd-tools 我一个人迭代到 v2.20，从 PRD 蒸馏到证据门禁做了二十多个版本；另外还独立做了两个 Agent 项目：一个 39 节点的多智能体编排总管，一个 18 工具的工程化 Coding Agent。这些都不是公司任务，是我想搞清楚 Agent 工程化到底怎么落地，自己逼自己快速试错。大厂给我的是工程素养和稳定性底线，创业要的『快速验证、错了就回滚』我在这几个项目里每天都在做——比如我的 Coding Agent 有 sha256 乐观锁和原子写盘，就是为了让 AI 改仓库时我能放心地让它快速试。」

要点：① 用个人项目证激情 ② 用 Agent 项目里的「快速试错+安全兜底」呼应创业 ③ 不提大厂负面。

---

### Q2｜技术｜日常用哪些 AI 工具和模型，对比 Agent 场景优劣
> 这是预测答案翻车最严重的一题（原答案乱点 Claude/GPT-4/Gemini/AutoGPT/CrewAI）。

❌ 别说：「Claude 3 Opus 做 Coding Agent 核心模型、GPT-4 做 Supervisor」——你实际 Agent 跑的是 qwen。
✅ 安全版：讲**真实的模型栈 + 真实的框架选型对比**。

**可背答案**：
> 「我分场景用：研发工作流我用 Claude——我的 prd-tools 就是 Claude Code 插件，Claude 在长上下文理解、多模态（直接看 PRD 截图）和指令遵循上最稳，适合做需要严谨证据链的蒸馏。但**部署的 Agent 我用的是阿里云 DashScope 的 qwen 系列**（qwen3.5-flash 做编排路由、qwen-coder-plus 做代码），核心考量是成本和延迟——编排层每个请求要调多次 LLM，flash 模型把单次成本压下来才跑得起 multi-agent。
>
> 框架我主力用 LangGraph，对比其他：它是显式状态机，有 checkpoint、条件边、recursionLimit，适合有分支/回环/HITL 的复杂编排，我的 Manager 就是一张 39 节点图；LangChain 更偏链式组合，复杂状态管理弱；AutoGPT 是单 Agent 自主循环，可控性差，不敢用在能写文件的场景；CrewAI 适合简单的角色分工，但复杂 DAG 编排和安全性不够。所以涉及到『能改仓库』『要人工确认』的场景，我只会选 LangGraph。」

要点：① 模型分场景（Claude 研发 / qwen 部署）显成本取舍 ② 框架对比落到自己的真实选型理由 ③ 不评价没用过的。

---

### Q3｜技术｜Agent 开发最常用什么框架？为什么选型？
> 原答案基本对（LangGraph + 39 节点 + checkpoint），但 checkpoint 部分要软化。

❌ 别说：「Custom CheckpointSaver 实现文件+Redis 双后端存储，支持跨进程续跑」（连用易翻车，见 §2⚠️）。
✅ 安全版：

**可背答案**：
> 「最常用 LangGraph。选它三个理由：第一，多 Agent 编排本质是有分支、回环、HITL、失败恢复的**状态机**，不是单次函数调用——我的 Manager 总管是一张 39 节点的图，所有旁路（澄清、人工确认、自愈回环）都用条件边谓词挂回主干，节点本身是纯函数，可单测可复用；第二，它有 checkpoint 和 recursionLimit，长周期任务能续跑、fix→synth 回环能防死循环；第三，LangSmith trace 让多 Agent 问题能定位到每条边为何被选。
>
> 跨进程续跑这块我要诚实说：真正稳定的 LangGraph 原生 checkpointer 我用的是 Postgres（thread_id 绑 session），默认是关的；我还自建了一套人工确认快照（16 个业务字段的子集，file/redis/dual 三后端、24h TTL），专门服务高风险写操作的 HITL resume。但 dual 模式目前是 best-effort、没有分布式锁，这是我下一步要补的工程债。」

要点：① 状态机理由 + 39 节点 ② checkpoint 拆成「Postgres 原生 saver（默认关）+ 自建 HITL 快照」讲，主动暴露 dual 无锁的债（加分）。

---

### Q4｜场景·困难｜为创业团队设计 Coding Agent 的全生命周期
> 原答案结构没问题，但别用编造的「效率+30%、通过率+20%」目标。

✅ 安全版（用你 code_assistant 的真实设计落地）：

**可背答案**：
> 「我会按六步走，而且每步都套我自己做 code_assistant 踩过的坑：
> 1. **需求对齐**：创业团队痛点是『AI 改代码怕出事』，所以第一性原理定为**模型 + 写权限 = 事故**，安全是地基不是事后补。
> 2. **架构**：LangGraph ReAct 双节点（agent ⇄ tools）；前端 Monaco + Diff 预览；按 task_kind 四路分发（compute/inspect/edit/full），只读请求不给写工具，省 token 又降风险。
> 3. **原型**：先上 11 个只读工具（读文件、AST、向量检索、git 只读），跑通『读仓库→定位→分析』；再加 7 个写工具。
> 4. **安全落地**（重头）：JWT+scope 鉴权、IP 限流、路径白名单+敏感文件拦截、sha256 乐观并发锁（冲突 409）、subprocess/docker 沙箱、原子写盘、写后自动 lint/typecheck/test + 自修复。
> 5. **上线**：chat-only 只读模式做灰度，审计日志全留，Monorepo 管理。
> 6. **持续迭代**：Prompt shadow→A/B 自进化（双信号：负反馈 + validate 失败才晋级）；建 benchmark oracle 做 P0 回归门禁，保证进化不把系统改坏。
>
> 量化目标我会先建基线再定，不会拍脑袋——比如先用 oracle 跑一批 case 出 requirement_recall / code_anchor_accuracy 的当前水位，再设提升目标。」

要点：① 全生命周期 ② 每步落到 code_assistant 真实机制 ③ 量化目标「先建基线再说」显严谨。

---

### Q5｜行为｜和产品需求分歧/频繁变更怎么处理
> 原答案全是编造数字（7→3 天、点击率+15%、2.8s→1.2s）。**全部删**。

✅ 安全版（用真实的中台/权益组件经验，只保留可举证的机制）：

**可背答案**：
> 「我的原则是先对齐产品的核心目标，再给技术替代方案，用机制而非情绪解决分歧。在营销中台做权益组件时，产品想要很灵活的多业务线配置，我认同他『提升配置效率』的目标，但我没直接堆组件数量，而是做了一个**可配置组件库 + Register 注册模式**：SDK 内置通用组件，业务方运行时注册定制组件、靠配置组合覆盖国家/业务线差异——这样不用为每个场景 fork 改源码。
>
> 这种分歧的处理方式是：把『需求变更』前置成『配置项』，产品后续要调权益展示，改配置就行、前端零代码不发版。结果是新增活动上线从两周缩到几小时，维护成本降了一半。具体的转化率/性能数字我们没有系统化建过三档基线对比，这是我承认的短板，计划补 Lighthouse + Web Vitals 把基线建起来。」

要点：① 先共情目标 ② 用「配置化」机制消化变更（真实） ③ 主动承认没建性能基线（诚实加分，别编）。

---

### Q6｜技术｜Agent Safety 实践？怎么解决编码幻觉和高风险操作？
> 原答案把 prd-tools 的 Citation Grounding 和 code_assistant 的 Read-before-Write 混着说，且多处夸大。

❌ 别说：「错误计划率 40%→15%」「code_assistant 有 Diff 预览用户确认才执行 + 代码审查 Agent」（后者代码里没有）。
✅ 安全版（分两层讲，都落到真实机制）：

**可背答案**：
> 「我在两个项目里分别从『规划』和『执行』两层做 Safety。
>
> **规划层（prd-tools）防幻觉**：核心是证据链——每条 requirement、影响、契约结论都必须挂一个 `EV-XXX` 证据 ID（来源是源码/契约/PRD/git diff/negative search），没有证据的结论进不了 spec；而且有一个**代码级硬门禁 Spec Review Gate**，spec 不被 owner 确认 approved，下一阶段的 quality-gate 直接 exit 2，流程硬停。LLM 不允许推断，只能引用已收集的事实。
>
> **执行层（code_assistant）防事故**：第一性原理是『模型 + 写权限 = 事故』，所以约束不放 prompt 里、放在代码同步路径上——sha256 乐观并发锁（文件被改过就 409 拒写）、路径白名单 + 敏感文件拦截（.env/id_rsa 摸不到）、subprocess/docker 沙箱跑校验、原子写盘、写完自动跑 lint/typecheck/test 自修复。读检索工具的返回天然带 file:line，系统 prompt 要求基于工具事实回答、不臆测。
>
> 我要诚实说：完整的『Diff 预览 → 用户确认才落盘 → 第二个审查 Agent 终检』这套是我设计目标里还没全落地的一块，目前是 chat-only 只读降级 + 审计日志 + 写后自动校验在兜底，HITL 写确认是我下一步要补的。」

要点：① 规划层证据链 + Spec Gate（真硬门禁）② 执行层四层约束（真实现）③ 主动暴露 HITL 写确认未全落地（诚实加分）。

---

### Q7｜行为｜主动推动过产品/体验优化？业务价值？
> 原答案编造「配置 2h→15min、错误率 20%→5%、5 条业务线」。**全删**。

✅ 安全版（用真实的 Schema 引擎 + 权益 SDK 故事）：

**可背答案**：
> 「最大的主动性体现在『从业务里抽共性做架构』。在营销中台，我注意到每次上新活动前端都要改代码发版，周期两周起，运营被卡。我主动去理解运营到底在配什么，抽象出 Schema 驱动的方案：BFF 模板引擎生成 Schema，前端渲染引擎动态消费，运营配 Schema 就能上活动、前端零代码不发版——联动用字段有向图建模，引擎底层解决依赖图 + 拓扑排序 + 循环检测。
>
> 这套引擎我还主动推成第二个业务的底座——权益组件库 benefit SDK 通过 Register 注册模式接入，30+ 组件靠配置组合覆盖多国家多业务线。结果是新增活动上线从两周降到几小时，一套引擎服务两个业务、维护成本降一半。性能基线我们还没系统化建三档对比，这块我承认是短板。」

要点：① 主动发现→抽象→复用 ② 只用可举证数字（2 周→几小时、维护成本降一半、30+ 组件） ③ 承认性能基线短板。

---

### Q8｜技术·困难｜Self-Evolution 怎么平衡迭代效率和稳定性？失败案例？
> 原答案机制大体对，但「准确率提升 10%/下降 5%/提升 8%」是编造的具体数字，要软化。

⚠️ 软化：具体 lift 百分比别报死数（除非真跑过实验），讲机制 + 非对称门槛。
✅ 安全版：

**可背答案**：
> 「我用四个机制平衡：① **假设节流**——LLM 进化假设每 30 分钟一批、有硬上限，避免频繁改动抖动；② **金丝雀分桶**——新策略先进 shadow，sessionId 用 sha256 稳定分桶只放 5% 流量，爆炸半径可控、同会话一致；③ **A/B 非劣性设计**——双臂各 ≥8 样本才评估，晋级门槛低（正向 lift 显著就上）、回滚门槛高（小幅负向不打回去），避免已验证策略被小波动反复打回；④ **自动回滚兜底**——新版本劣化超阈值自动 restore。
>
> 平衡的关键是『只调阈值和注入 prompt 短规则、不生成新代码』——可回滚、可解释、不引入编译期错误。
>
> 失败案例：我试过优化总管的意图路由逻辑，新假设在 A/B 里 compositeScore 出现负向 lift，我立即回滚，复盘发现是 few-shot 经验样本不够、把不相关的历史路径注进了 router。修复方式是给经验回放加时间衰减（约 7 天半衰）和负样本隔离，重新跑才通过。具体数字我现在不报精确值——因为样本量还在攒，我更愿意讲机制和门槛设计，不想报一个经不起追问的百分比。」

要点：① 四机制 + 「只调阈值不生成代码」② 真实失败案例（路由优化回滚）③ 主动不报精确 lift 数字（诚实，避免被追问穿帮）。

---

## 5. 项目深挖准备（3 个，含 killer followups 与红线）

### 项目 1：Manager_Agent（多智能体编排总管）

**真实定位**：14 个专业 Agent（RAG/NL2SQL/代码/爬虫/GUI/办公/多模态…）之上的编排层。统一 WS 入口，用户一句话 → 理解意图 → 路由 → 多步 DAG → 并行调度 → 汇总 → critic 审计 → 自我进化。

**killer followups 准备**：
- 「画状态机主干」→ START→resource→tool_health→probe→metacog→security_gate→**decompose→intent_classify**→route→prefetch→(single 直跳 / multi 走 planner→scheduler→plan_lint→[plan_preview]→multi)→synth→critic→optimizer(可回环 fix)→verifier→finalize→END。**强调 decompose 在 intent_classify 前**（先拆子句再分类，避免一句两意图取平均）。
- 「路由会不会错乱」→ 三重防御：userIntentSupremacy 能力漂移降权(*0.35)、stripAdmin 剔除误带、planLinter 越权检查。
- 「Bandit/RL/Causal 三套冲突谁说了算」→ 都只产 routerHintBlock 文本，LLM 综合决策；用户意图最高。
- 「shadow→promote 怎么不把系统改坏」→ 四闸门：物理隔离 0% 流量、5% 金丝雀、A/B minSamples8 + lift 非对称、verifyBeforePromote + confidence 阈值（policy 0.72 / prompt 0.68 / planner 0.65）+ 自动回滚。
- 「reducer 用错的隐蔽 bug」→ entities 故意用覆盖（非累积），否则上周的人名污染今天的 planLint。

**红线 ❌**：别说「Supervisor 模块」（叫总管/Router）；别说失败归因是 LLM 做的（纯规则）；别连用「Custom CheckpointSaver + 跨进程续跑」。

### 项目 2：code_assistant（工程化 Coding Agent）

**真实定位**：Cursor 类工程化 Coding Agent——读仓库→语义检索→AST 静态分析→Bug 扫描→Diff 预览→受控写盘。第一性原理「模型+写权限=事故」。

**killer followups 准备**：
- 「18 工具具体功能」→ 只读 11（list/read/semantic_search/vector_search/git 只读×4/ast_analyze/analyze_dependencies/remember）+ 写 7（write_file/apply_diff/validate_project/run_tests/git_commit/git_create_branch/generate_docs）。补一句「只读模式收敛到 11」。
- 「read-before-write 怎么强制」→ 真实是：apply_diff 内部先读旧内容再算补丁；write_file 靠 sha256 乐观锁；安全靠 JWT/scope/限流/路径白名单/敏感文件拦截/沙箱/原子写/审计。**诚实说 HITL 写确认未全落地**。
- 「代码 RAG 怎么做」→ 自研：text-embedding-v3 + 内存余弦 + sha256 内容指纹惰性增量（查询时按需重算）。**别说 Chroma、别说 git 钩子**。
- 「AST 用什么」→ `@typescript-eslint/typescript-estree`（**别说 web-tree-sitter**）。

**红线 ❌**：别说 Chroma/Sentence-BERT/OpenAI embedding（实为自研+text-embedding-v3）；别说 git 钩子增量；别说 Diff 预览用户确认才执行/代码审查 Agent/HITL 写确认（未实现）；别说 Node vm 沙箱（实为 subprocess/docker）。

### 项目 3：prd-tools（PRD-to-Code 上下文编译器）

**真实定位**：Claude Code 插件，两个 skill `/reference`（建知识库）+ `/prd-distill`（PRD 蒸馏成 spec/plan + 证据）。对标 GitHub Spec Kit，差异在「从存量代码/契约证据编译上下文」。**不生成代码**。

**killer followups 准备**：
- 「证据链怎么做」→ 每条 requirement/impact/contract 挂 EV-XXX/GCTX-XXX ID，8 种证据类型（prd/tech_doc/code/git_diff/negative_code_search/human/api_doc/reference）。
- 「Spec Review Gate」→ 代码级硬门禁，spec-confirmation.yaml 未 approved 则 quality-gate exit 2。
- 「Reference 六文件」→ codebase/coding-rules/contracts/routing-playbooks/domain/project-profile，SSOT，跨文件用 ID 引用。
- 「五维 quality-gate」→ required_files/context_pack_consumed/code_anchor_coverage/plan_completeness/blocker_quality（**别说安全性/可维护性**）。
- 「知识怎么抽取」→ rg/glob 定位 + Claude Read 源码确认 + 正则版 Evidence Index 做确定性锚点；早期接过 GitNexus AST 但 v2.13 已移除换回原生。

**红线 ❌**：别说 SDD Platform/端到端代码生成/prd2code-gen/6 步闭环/AST/ESLint/独立 agent 终检/20+ 团队/40%→15%/缓存定价数字。

---

## 6. 技术复习清单（按优先级）

**🔴 必背（高概率考）**
- LangGraph：状态机、条件边谓词、Annotation reducer 的 4 种合并语义（concat/覆盖/浅合并/last-wins）、recursionLimit + 业务 retryBudget 分层治死循环
- Agent Safety：read-before-write 四层约束、evidenceGate、Spec Review Gate、HITL 两套（进程内阻塞 vs 跨进程 checkpoint）
- Multi-Agent：Supervisor/Orchestrator 模式、Plan-and-Execute、ReAct（ToolNode + function calling 语义）
- Self-Evolution：shadow→金丝雀→A/B 非劣性设计（lift +0.03/−0.06）、只调阈值不生成代码、自动回滚
- RAG：向量+Jaccard 混合召回、sha256 增量索引、embedding 选型（text-embedding-v3）
- LLM 评估：LLM-as-Judge、Golden Set / benchmark oracle、false_positive_penalty

**🟡 适配产品协作（中）**
- React + TypeScript；Server-Driven UI / Schema 驱动渲染；Monorepo；SDK 工程化（Rollup 双模 + peerDeps + Changesets）
- 字段有向图依赖：依赖图建模 + 拓扑排序 + 循环检测

**🟢 系统设计**
- Coding Agent 全生命周期（§4 Q4）
- 创业场景快速迭代 = 「快速试错 + 安全兜底」（用 sha256 锁/原子写/沙箱/A/B 回滚串起来）

---

## 7. 临场红线速查（进场前过一遍）

1. **任何带箭头的性能/效率百分比，先问自己「这数字哪来的」**——查无证据的（40%→15%、+15%、2h→15min、20%→5%、5h→2h）一律不报，改说机制或承认没建基线。
2. **prd-tools 永远别说**：SDD Platform、prd2code-gen、端到端生成、20+ 团队、AST/ESLint、独立 agent 终检、缓存定价数字。统一口径：**Claude Code 插件 / PRD-to-Code 上下文编译器 + 证据门禁**。
3. **模型栈**：研发用 Claude（prd-tools）、部署用 qwen（Agent 项目）。别说 GPT-4/Claude 是你 Coding Agent 主力。
4. **五维 quality-gate** 按 §3 真实维度答。
5. **Checkpoint** 别连用「Custom CheckpointSaver + 跨进程续跑」，拆成 Postgres 原生 saver（默认关）+ 自建 HITL 快照（16 字段）。
6. **code_assistant 安全**讲真实现（JWT/scope/限流/路径白名单/sha256 锁/沙箱/原子写/审计），别讲未实现的 HITL 写确认/审查 Agent。
7. **被追问到没做过的**：直接说「这块我目前是 X 在兜底，Y 是我下一步要补的」——主动暴露取舍比假装完美强，这是你资料里反复强调的加分答法。

---

> 一句话总结：你的项目都是真材实料（39 节点、A/B 进化、证据闸门、sha256 锁全是真代码），**只要把编造数字和包装话术换掉、改讲真实机制**，明天稳。最大的敌人不是面试官，是预测答案里那些立不住的数字。
