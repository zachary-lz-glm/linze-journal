# 面试急救包 — 速记版

> **这是什么**：八股、算法、话术的压缩版，只保留能脱口而出的内容
> **什么时候看**：面试当天或前 1 天，快速过一遍核心句
> **怎么看**：每块只背**加粗句**，其余看一眼有印象即可

---

## 零、自我介绍（必考第一题）

### 1 分钟版本（标准）

> 面试官你好，我是邓泽霖，**四年大厂前端经验，目前在滴滴国际化事业部负责营销中台的前端架构**。之前的工作主要集中在三个方向：
>
> **第一是 Schema 驱动的全栈架构。**我设计了一套 BFF 动态生成 + 前端动态渲染的 Schema 驱动架构，核心是一个双重求值的联动引擎——BFF 端先算一次保证初始状态正确，前端运行时再算一次做联动。覆盖了 20 多种活动类型，**新增活动前端零代码，上线周期从两周缩短到三天**。
>
> **第二是权益组件库。**主导了 400 多种营销权益的 React 组件库，用 Register 注册模式 + Schema 同构渲染，和活动表单共享同一套渲染架构。覆盖了 8 个业务项目，**新权益接入从两天缩短到两小时，零 breaking change 跑了 18 个月**。
>
> **第三是 AI 工程工作流。**独立设计了一套 PRD 蒸馏工作流，核心不是让 AI 直接写代码，而是把 PRD 转成有证据、可追溯的结构化开发计划。用 6 文件 SSOT 知识库 + 8 种证据类型 + 多层质量门控保证可靠性，**PRD 转化效率提升 60% 以上**。
>
> 我的**核心优势是擅长从业务中抽取共性做架构抽象**，以及对全流程质量的把控。以上是我的自我介绍，希望和您有进一步沟通。

### 30 秒版本（追问/加面/时间紧）

> 面试官好，**我是邓泽霖，四年前端，在滴滴国际化做营销中台架构**。核心做三件事：**一是 Schema 驱动架构**，BFF 生成 + 前端渲染 + 双重求值联动引擎，新增活动零代码；**二是 400+ 权益组件库**，Register 注册 + Schema 同构，8 个项目接入；**三是 AI 工程工作流**，PRD 蒸馏 + 证据链 + 质量门控，PRD 转化提效 60%+。

---

## 一、八股口语化速记（5 题，每题 3 句话）

### #1. React 18 并发 + Fiber

1. **Fiber 本质是把虚拟 DOM 从树变成链表**（child/sibling/return），这样渲染可以随时中断和恢复
2. 之前是一递归到底不可中断，**现在通过 Scheduler 用 MessageChannel 按优先级调度**，高优先级任务（比如用户输入）可以打断低优先级渲染
3. React 18 新增 useTransition 标记低优先级更新、useDeferredValue 延迟渲染、**Automatic Batching 自动合并多次 setState**，不管在什么上下文里都只触发一次渲染

### #2. useEffect vs useLayoutEffect

1. **useLayoutEffect 在浏览器绘制前同步执行，会阻塞渲染；useEffect 在绘制后异步执行，不阻塞**
2. 执行顺序是：DOM 更新 → useLayoutEffect → 浏览器绘制 → useEffect
3. 所以**需要读 DOM 布局或做同步修改用 useLayoutEffect**（防止闪烁），其他都用 useEffect

### #4. 浏览器渲染管线

1. **HTML 解析成 DOM，CSS 解析成 CSSOM，合并成 Render Tree**
2. 然后 Layout 算几何位置 → Paint 画像素 → Composite 合成图层
3. **改 width/height 触发 Reflow（重排最贵），改 color 触发 Repaint，只改 transform/opacity 只触发 Composite 性能最好**

### #5. HTTP 缓存

1. **强缓存不发请求**，通过 Cache-Control 的 max-age 直接用本地缓存
2. **协商缓存发请求但可能 304 不下载内容**，用 ETag/If-None-Match 做精确比对
3. 实战配置口诀：**HTML 用 no-cache 每次协商、带 hash 的静态资源 max-age 设一年、API 用 no-store 不缓存**

### #12. TypeScript 高级类型

1. **infer 是在条件类型里"推断"一个子类型**，比如从函数类型里推断返回值类型
2. 条件类型就是类型层面的三元表达式 `T extends X ? A : B`
3. 映射类型可以遍历联合类型生成新类型，**Pick、Omit、Partial 本质都是映射类型**

---

## 二、算法万能口诀（详细模板见 [手写代码.md](手写代码.md#四算法模板5-题)）

> 10 个套路，每个一句话，面试前过一遍

| 场景 | 套路 | 口诀 |
|------|------|------|
| 找配对/重复 | 哈希 Map | 查到返回，查不到存 |
| 两端夹击 | 双指针 | 排序后左右夹 |
| 层次/最短 | BFS 队列 | 逐层处理 |
| 子串/子数组 | 滑动窗口 | 右扩左缩 |
| 排列/组合 | DFS 回溯 | 选→走→撤 |
| 树路径 | DFS 带参 | 带参数往下传 |
| 合并有序 | 从后填 | 大的先放 |
| 字符串模拟 | 按位处理 | 注意进位 |
| 最大子序 | DP 贪心 | 接前面或重新开始 |
| 排序 | 快排分治 | 原地 partition |

---

## 三、不会答时的体面话术

### 级别 1：有点印象但说不准确

> "这个知识点我之前在项目中遇到过/看过相关源码，**我理解的核心思路是 [说你能想起来的]**，但具体到实现细节我可能说得不够精确。不过从使用场景来看，我觉得它主要解决的是 [猜一个合理场景] 的问题"

### 级别 2：完全没概念（算法题不会做）

> "这道题我一时想不出最优解，**但我先说一下我的初步思路**——[说暴力解法或部分想法]。如果面试时间允许，我想请问一下这道题期望的时间复杂度是多少？这样我可以往那个方向优化"

**关键：永远不要说"我不会"就停了，永远要给出下一步动作**

### 级别 3：被追问到死角

> "这个问题确实触及了我的知识盲区，**我之前的理解可能不够深入**。我回去会重点补一下这块。不过基于我目前的项目经验，我的理解是 [说一个合理的推测]，不知道这个方向对不对？"

### 级别 4：系统设计完全没思路

> "这个场景我确实没有直接设计过，**但我可以从我做过的 Schema 驱动架构类比一下**。核心都是分层解耦 + 可扩展性问题。如果让我来设计，我会先确定这几个关键问题：[列 2-3 个关键设计决策点]"

### 万能兜底（任何场景都能用）

> **"这个问题我实际遇到过..."** → 锚定你的项目经验，把话题往你熟悉的领域引导

> **"如果让我重新设计..."** → 展现反思能力，即使答得不好也能加分

---

## 四、CodeTop 高频补充（完整代码见 [手写代码.md](手写代码.md)）

> 来源：CodeTop (codetop.cc) 字节+美团+快手前端岗实际面经统计
> 必做 5 道：快排、合并有序数组、字符串相加、最大子序和、二叉树路径

---

## 五、项目关键数据速查（面试必背）

> 每个数字都要能脱口而出，面试官随时可能问
> 每行后面的「口径」= 被追问"怎么算的"时的 30 秒回答

### 简历数据（高风险追问）

| 简历指标 | 数据 | 口径（被问怎么算的） |
|---------|------|-------------------|
| PRD 转化效率 | **+60%** | 基线：人工读 PRD + 开会确认 → 出开发计划平均 5h/需求；蒸馏后自动出结构化计划 2h/需求（不含人工确认），取 3 个月均值对比 |
| 返工减少 | **50%+** | 口径：需求评审后到提测前，因需求理解偏差导致的返工工单数。前 3 个月 12 单 → 后 3 个月 5 单。返工定义：开发过程中需重新和 PM 对齐需求导致代码回退 |
| 领域知识条目 | **200+** | 口径：reference 6 文件中去重后的独立事实条目数（enum 值、字段契约、编码规则、业务术语各算一条），由 `build-index.py` entities.json 统计 |
| 上线周期缩短 | **2 周→3 天** | 之前：PM 写 PRD → 前后端各开发联调 3-5 天 → 测试 2-3 天。之后：PRD 蒸馏自动出计划 → BFF 配模板 + 联动（半天）→ 前端零代码 → 测试 1-2 天。缩短在开发阶段 |
| 联动复用率 | **80%+** | 口径：23 种活动类型的联动需求中，19 种可复用已有 linkageSchema（通过 handler 提取复用），仅 4 种需新增联动文件 |
| 覆盖业务项目 | **8 个** | 营销平台（dive）、司机端（captain）、乘客端（ride_pass）、权益管理（benefit-sdk）、运营工具、数据看板、A/B 实验平台、国际化配置中心 |
| 零 breaking change | **18 个月+** | 策略：SemVer 严格遵循 / peerDependencies 隔离 react / deprecated 标记不删除 / Changesets 追踪跨包影响 / alpha 联调后才 release |
| 新权益接入 | **2 天→2 小时** | 之前：前端写组件 + 后端写接口 + 联调。之后：后端在 MIS 配 Schema（d_component 映射已有组件）+ 前端零代码，2 小时是后端配置时间 |

### 架构数据（中风险追问）

| 指标 | 数据 | 补充说明 |
|------|------|---------|
| 权益数量 | **400+** | 累计接入的权益 SKU 数（不同国家 × 不同类型），不是 400 个组件 |
| 联动类型 | **16 个** | 12 重渲染型（从 render 模板提取 handler）+ 4 Placeholder 重置型（只清空 value） |
| 组件数量 | **35+** | 24 内置 Register（通用 16 + 业务 8）+ 运行时扩展 11+ |
| 活动类型 | **20+** | 简历用 20+，和 23 种说法一致，按统计口径不同 |
| Monorepo 模块 | **15+** | app/ 目录 15 个业务子包 + packages/ 共享包 |
| BFF 模板 | 5 步 + 16 联动 | basic/group/rules（25 子模板）/message/preview + 16 linkageSchema |
| 打包产物 | ESM + CJS + .d.ts | Rollup 三输出，`exports` 字段条件导出 |
| prd-tools | 8 证据 / 5 评分 / 6 文件 | 8 种：code/prd/tech_doc/git_diff/negative_code_search/human/api_doc/reference |

### 治理数据（低风险）

| 指标 | 数据 |
|------|------|
| 误报率优化 | **降 83%**（四层错误码过滤后只报前两层） |
| 线上问题治理 | 一个月内解决 10+ 起，归纳为 5 种类型 |
| 能力面 | 前端 9 / BFF 10 / 后端 10 = **29 个**（简历写 20+ 是保守口径） |

---

## 六、接入层三种形态（1 分钟精炼版）

> ⚠️ 阿里二面被问到，回答散乱，用这个版本

```
三种形态，按消费方需要的控制力度区分：

1. 组件模式（<App> 内嵌）
   → 消费方需要完整 CRUD 能力
   → import 组件 + 传参，整个权益生命周期都交给你

2. 函数调用模式（Modal.info 命令式）
   → 只需要单点能力（比如查一个权益详情）
   → 暴露 Modal 方法，不需要引入整个组件
   → BenefitAdmin 统一分发，解耦内部实现

3. 微前端模式（qiankun 挂载）
   → 不限版本，每次跟最新
   → 权益系统整体作为一个子应用，嵌入营销中后台
   → 适合不需要定制、直接用标准能力的场景
```

---

## 七、面试前 5 分钟速览

```
八股：Fiber=链表可中断 / useLayout=同步阻塞 / React19=RSC+Compiler / 渲染=DOM+CSSOM→Layout→Paint→Composite / 缓存=强缓存不请求+协商304 / HTTP2=多路复用 / 事件循环=微先宏后 / XSS=注入+CSRF=冒充 / CSS=BFC/Flex/Grid / TS高级=infer推断+条件三元+映射遍历 / 原型链=__proto__逐级查找到null / this=箭头继承外层+普通看调用者

项目数据：400+权益 / 16联动(12重渲染+4Placeholder) / 35+组件(24内置+运行时) / 23活动类型 / 15+业务模块 / 误报降83% / injectDActions双重求值 / BFF模板引擎驱动 / Rollup三输出 / prd-tools 8证据+5评分+6文件SSOT

项目速记：重构=问题治理1月+架构升级3月+规范落地1月 / BFF=聚合+适配+兜底 / 联动=injectDActions双重求值( BFF safeEval + 前端eval) / 规范=开发规范+流水线+需求流程 / 运维=双层感知(主动监控+K8s探针)+TraceID全链路 / 设计模式=策略去if-else+观察者做联动+工厂生成Schema+柯里化预设参数

微前端：qiankun=single-spa+JS沙箱(Proxy)+样式隔离(Shadow DOM)+HTML Entry(fetch解析执行) / 渐进式升级=Schema加version+运行时动态import+配置中心灰度

算法口诀：找配对→Map / 两端夹→双指针 / 层次走→BFS队列 / 子串→滑动窗口 / 排列→回溯DFS / 树路径→DFS带参数 / 合并有序→从后填 / 字符串→按位处理 / 最大子序→DP贪心 / 快排→分治

手撕必会：深拷贝+防抖节流+instanceof+EventEmitter+call/apply/bind+Promise调度器+模板编译+MoveZeroes双指针

话术：说不出来就说思路 → 再不行就问提示 → 实在不行承认盲区但说回去会补 → 永远把话题往你做过的项目引
```
