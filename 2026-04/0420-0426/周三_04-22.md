# 2026-04-22 周二 — Reference 领域知识标准化 & PRD 蒸馏优化

## 一、调研背景

bff-gen 要推广到 B 端全栈（前端/后端/BFF），面临两个核心挑战：

1. **Reference 领域知识标准化** — 当前是单层（BFF）× 单项目（marketing-bff），无法扩展
2. **PRD 蒸馏质量优化** — 蒸馏效果最大瓶颈是 PRD 输入质量（★★★★★），而非蒸馏算法

**研究方法**：代码库深度分析 + 30+ 篇行业文献调研 + BMAD Architecture 阶段正式产出

---

## 二、行业趋势：Context Engineering

2025-2026 年，AI 代码生成的核心演进方向是 **Context Engineering**——系统化地构建和管理提供给 AI 的知识架构。

| 趋势 | 说明 |
|------|------|
| Context Engineering 作为学科 | 迭代式构建、优化、测试 AI 的输入 |
| Knowledge Priming | Martin Fowler："知识预注入"——AI 生成代码前先共享策划好的上下文 |
| 3-Tier Knowledge Architecture | 企业级 AI coding agent 采用 3 层知识架构（共享/领域/任务） |
| Virtual Monorepo Pattern | 跨 35 个仓库为 Claude Code 提供全系统上下文 |
| .md 作为核心工程资产 | Markdown 文件成为 AI 协作中最有力的工程制品 |

### 5 层上下文架构（行业共识）

| 层级 | 目的 | bff-gen 对应 |
|------|------|-------------|
| 指令层 | 清晰具体的指令 | `steps/*.md` |
| 领域层 | 领域特定知识 | `reference/` |
| 共享上下文层 | 多代理共享主干 | 待建设 |
| 示例层 | Few-shot 示例、模板 | `templates/*.hbs` |
| 工具层 | 可用的 API、能力 | `validators/` + `installer/` |

### AI 工具知识管理对比

| 工具 | 格式 | 层级 | 置信度 |
|------|------|------|--------|
| Cursor | `.mdc` | 单层 | 无 |
| Claude Code | `CLAUDE.md` | 多层 | 无 |
| **bff-gen** | `reference/*.md` + Schema | **多层 + 校验** | **✅ confidence 3级** |

---

## 三、PRD 蒸馏优化

### 蒸馏效果 7 因素分解

```
工具效果 = reference/ 质量 × PRD 质量 × LLM 能力
```

| # | 因素 | 权重 | 当前 | 优化方向 |
|---|------|------|------|---------|
| 1 | **PRD 结构化程度** | 25% | 低 | PRD Schema 1.0 |
| 2 | **Reference 覆盖度** | 25% | BFF 75% | 三层标准化 |
| 3 | **字段映射明确性** | 15% | 中 | PRD 显式标注 target_file |
| 4 | **PRD-Reference 语义匹配** | 15% | 低 | PRD 用标准术语 |
| 5 | **后端契约可用性** | 10% | 低 | 强制后端文档 |
| 6 | **蒸馏 Prompt 质量** | 5% | 高 | 持续迭代 |
| 7 | **LLM 模型能力** | 5% | 中 | 模型升级 |

### 蒸馏 4 种错误类型

| 错误 | 根因 | 发生率 | 解法 |
|------|------|--------|------|
| 字段归属错误 | PRD 没标注字段属于哪个步骤 | ~30% | PRD 按步骤分区 |
| 组件类型误判 | "选择" = Select 还是 Radio？ | ~15% | PRD 标注组件白名单 |
| 遗漏字段 | PRD 信息分散 | ~20% | 表格化替代自由文本 |
| 编造信息 | LLM 倾向填满所有字段 | ~10% | 强调"未提及标 low" |

### AI 友好 PRD Schema 1.0

```markdown
## Meta | campaign_type | country | is_new_type | reference_campaign |
## 基础信息（basic）     | PRD 字段名 | dataIndex | 组件类型 | 必填 | 校验规则 |
## 奖励条件（rewardCondition）| 同上 |
## 规则详情（detail）    | PRD 字段名 | dataIndex | 组件类型 | 梯度类型 |
## 联动关系              | 字段 | 联动类型 | 依赖字段 | 联动逻辑 |
## 校验规则              | 规则 | 错误提示 | 作用范围 |
## 预览占位符            | 占位符 | 说明 |
```

核心设计：按步骤分区（归属准确率 30%→90%）+ 强制白名单选组件（准确率 95%）+ 表格替代自由文本（遗漏率 20%→5%）

### 蒸馏准确率提升路线

| 阶段 | 动作 | 准确率 |
|------|------|--------|
| 0（当前） | 自由文本 + BFF reference | ~70% |
| 1 | PRD Schema 1.0 | ~85% |
| 2 | 三层 reference 标准化 | ~90% |
| 3 | 联动显式 + 后端契约 | ~95% |

---

## 四、最终架构：9 文件标准 Reference 结构 ⭐

> 以下为 BMAD Architecture 阶段正式产出（已更新），否决了之前调研中的 `contract.json + domain/` 方案。

### 核心定位转变

**工具是流程化能力提供者，不是数据存储**：

```
用户维护 reference（领域知识）→ 工具读取 reference → 理解项目 + 蒸馏 PRD → 结构化开发文档
```

Reference 是用户的输入，不是工具的输出。工具的核心职责是**强制 AI 严格按用户注入的 reference 执行**，防止 AI 凭"经验"判断导致偏差。

### 为什么否决 contract.json + domain/ 方案

| 之前的方案 | 问题 |
|-----------|------|
| `contract.json` 作为共享契约 | 过于简单，只覆盖"字段注册表"和"术语表"两类，实际有效的 BFF reference 有 **8 类知识** |
| `domain/` 层无关知识 | 先不考虑跨层共享，各团队代码差异太大，纯靠自动生成不靠谱 |
| 工具自动从代码生成 reference | ❌ 各团队代码结构差异大，不可靠 |
| 工具从 git diff 推断规则 | ❌ 不可靠 |

### 8 文件标准结构

```
reference/
├── 00-index.md            # ⭐ 导航索引：场景 → 读哪些文件
├── 01-registry.json       # 结构化目录：有什么（字段、枚举、组件清单）
├── 02-topology.md         # 代码拓扑：放哪里（字段归属决策树、文件映射）
├── 03-conventions.md      # 命名规范：怎么命名（文件/代码/i18n/枚举）
├── 04-patterns.md         # 代码模式：怎么写（模板结构、handler 类型、hook 用法）
├── 05-constraints.md      # 约束规则：什么允许/禁止（白名单、校验规则、边界）
├── 06-anti-patterns.md    # 反模式：什么错误要避免（已知错误 + 检测方法）
├── 07-mechanisms.md       # 框架机制：系统怎么运作（内部行为、数据流边界）
└── 08-glossary.yaml       # 术语表：业务术语 ↔ 技术术语（PRD 关键词映射）
```

### 00-index.md 导航文件（新增）

AI 和人快速定位知识的入口，按场景映射到需要读取的文件：

| 场景 | 需要读的文件 |
|------|------------|
| 新 PRD 蒸馏 | 01 + 05 + 08 + 06 |
| 确定字段放哪个文件 | 02 + 01 |
| 写代码不确定命名 | 03 |
| Code review 检查 | 05 + 06 |
| 新人了解全貌 | 02 → 03 → 04 → 07 → 05 → 06 |

### Metadata 新鲜度追踪（新增）

每个文件包含 `last_verified` + `verify_cadence` + `owner`：

- JSON 文件：顶层字段
- .md 文件：YAML frontmatter（`---` 分隔）
- YAML 文件：顶层字段（避免 `---` 语法歧义）
- `ref-validate` 检查过期时报 warning（不阻塞）

### 工作流 6 步分别消费不同的 reference 文件

| 步骤 | 读取的文件 | 用途 |
|------|-----------|------|
| step-01 PRD 蒸馏 | 01 + 05 + 08 | 枚举白名单校验 + 术语映射 |
| step-02 项目分析 | 02 + 03 + 04 | 文件结构 + 命名规则 + 代码模式 |
| step-03 变更计划 | 02 + 04 | 字段归属 + 代码模式 |
| step-04 代码生成 | 03 + 04 + 07 | 命名规范 + 代码模式 + 框架边界 |
| step-05 验证修正 | 05 + 06 | 白名单硬校验 + anti-patterns 逐条检查 |
| step-06 输出报告 | （不读 reference） | — |

### 为什么是这 8 类

| 知识类型 | 占比 | 解决什么 AI 偏差 |
|---------|------|-----------------|
| Registry（目录） | ~15% | 编造枚举值、重复提取已有字段 |
| Topology（拓扑） | ~15% | 字段归属错误（basic vs rewardCondition） |
| Conventions（规范） | ~12% | 命名不一致、风格漂移 |
| **Patterns（模式）** | **~33%** | 违反代码模式、框架约定 |
| Constraints（约束） | ~14% | 使用白名单外的组件/函数 |
| Anti-patterns（反模式） | ~16% | 犯已知错误（options: null 等） |
| Mechanisms（机制） | ~10% | 误解框架行为、重复定义自动处理的字段 |
| Glossary（术语） | ~5% | PRD 术语 → 字段名映射错误 |

### 现有 BFF reference → 新结构映射

| 现有文件（6 个） | → 新文件 |
|-----------------|---------|
| field-mapping.md (186行) | 01-registry.json + 02-topology.md |
| naming.md (143行) | 03-conventions.md |
| scaffold-patterns.md (399行) | 04-patterns.md |
| validation.md (172行) | 05-constraints.md |
| anti-patterns.md (189行) | 06-anti-patterns.md |
| bff-hooks.md (117行) | 07-mechanisms.md + 04-patterns.md |
| (无) | 08-glossary.yaml **新增** |
| (无) | 00-index.md **新增** |

### 防偏差保障机制

工具的核心价值：**强制 AI 严格按 reference 执行，每个产出字段可追溯到具体规则**。

| 偏差类型 | 无 reference 时 | 有 reference + 工具强制时 |
|---------|----------------|----------------------|
| 字段归属错误 | AI 凭感觉判断 | 严格按 02-topology 决策树 |
| 命名不一致 | 每次生成不同风格 | 严格按 03-conventions |
| 编造枚举值 | AI 自由发挥 | 严格从 01-registry 白名单选择 |
| 违反代码模式 | AI 用自己的风格 | 严格按 04-patterns 模板 |
| 犯已知错误 | 重复 options:null 等错误 | 06-anti-patterns 逐条校验 |
| 误解框架行为 | 不知道后端自动 merge | 07-mechanisms 告知哪些不需定义 |

---

## 五、各层 Reference 内容

### BFF 层（已有，需迁移）

| 文件 | 核心内容 | 团队构建方式 |
|------|---------|------------|
| 01-registry.json | 字段列表 + 枚举（campaign_types, component_types, context_functions） | Claude Code 扫描代码库列出 |
| 02-topology.md | 字段归属决策树 + 目标文件映射 + 联动类型映射 | 开发者凭经验写 + 技术文档引导 |
| 03-conventions.md | 文件/代码/i18n key/枚举命名规则 | 从代码总结规律 |
| 04-patterns.md | 文件外壳 + 3 种 Handler + 各 step 特殊模式 | 开发者凭经验写 |
| 05-constraints.md | component 白名单 + context function 白名单 + VALID_TARGETS | 列出白名单和常量 |
| 06-anti-patterns.md | 10 个已知错误 + 检测方法 | 回忆踩过的坑 + Code review 高频评论 |
| 07-mechanisms.md | afterEach auto-merge + injectDActions + 三层边界 | 描述框架机制 |
| 08-glossary.yaml | 字段中文名 → id + PRD 关键词列表 | 对照历史 PRD 补充 |

### 前端层（基于 营销平台/app/dive 分析）

| 文件 | 核心内容 |
|------|---------|
| 01-registry.json | 组件清单（cmpName → 文件路径）+ state_paths + API 端点 |
| 02-topology.md | 组件注册流程 + Redux store 结构 + BFF schema → 渲染数据流 |
| 03-conventions.md | XxxCmp 组件命名 + PascalCase 目录 + props/CSS 命名 |
| 04-patterns.md | d_component → props 接线 + effect 模式 + 预览更新 |
| 05-constraints.md | 基础组件白名单 + 业务组件清单 + import 路径规则 |
| 06-anti-patterns.md | ⚠️ 待收集（需前端 team 反馈） |
| 07-mechanisms.md | Schema 驱动渲染 + Register 动态分发 + 活动类型切换数据流 |
| 08-glossary.yaml | 组件中文名 → cmpName + 业务术语映射 |

### 后端层（⚠️ 待后端团队访谈）

| 文件 | 预期内容 | 待获取 |
|------|---------|--------|
| 01-registry.json | API 端点 + 数据模型 + 枚举 | 技术栈、API 文档 |
| 02-topology.md | 路由注册 + 服务层结构 | 目录结构 |
| 03-conventions.md | API 路径/列/响应命名 | 命名习惯 |
| 04-patterns.md | Controller/Service/Repository | 代码分层 |
| 05-constraints.md | 数据类型 + API 版本 | 技术约束 |
| 06-anti-patterns.md | 待收集 | 已知坑点 |
| 07-mechanisms.md | options/attrs/errs 下发 + 数据聚合 | 字段来源边界 |
| 08-glossary.yaml | API 字段 ↔ 业务术语 | 术语映射 |

---

## 六、Reference 构建流程

### 场景 A：已有项目从零建 reference

```
Step 1: ref-init（初始化）
  npx bff-gen ref-init --layer <bff|frontend|backend> [--example marketing-bff]
  → 9 文件骨架（00-index.md + 8 个知识文件，含格式说明 + metadata）

Step 2: 填充内容
  工具提供: 8 个通用 prompt 模板 + marketing-bff 完整示例
  团队执行: 用 Claude Code / Cursor / 手工填写
  → 9 个填好的 reference 文件

Step 3: ref-validate（校验）
  npx bff-gen ref-validate --target /path
  → 格式合规 + 一致性报告
```

### 场景 B：新项目，参考项目无 reference（新增）

8 个知识文件中 **5 个是技术栈层面的知识**（02~07），可通过 prompt 分析参考项目代码提取：

| 文件 | 新项目处理 | 来源 |
|------|-----------|------|
| 01-registry.json | **清空**，从空开始 | 随代码生成增量填充 |
| 02~07 | prompt 分析参考项目提取 | 框架层面知识 |
| 08-glossary.yaml | 从 PRD 构建 | 术语映射 |

### ref-validate 三级校验（新增）

**级别 1：格式校验** — 9 文件齐全、JSON/YAML 合法、.md 非空

**级别 2：同层内部一致性**
- registry 枚举无重复
- glossary 的 enum_value / field_id 在 registry 中存在
- metadata freshness（last_verified + verify_cadence 过期报 warning）

**级别 3：内容新鲜度**（`--lint` 模式）
- registry 实体在代码中仍可找到
- constraints 枚举与代码实际枚举匹配
- patterns 路径仍有效

**不做**：跨层校验、内容正确性、语义校验

### CLI 精简为 2 个命令（从 3 个简化）

| 命令 | 作用 |
|------|------|
| `npx bff-gen ref-init` | 创建 9 个空文件 + metadata 头 |
| `npx bff-gen ref-validate --target /path` | 格式+一致性校验（3 级） |

`ref-prompts` 不单独命令——prompt 直接放 `src/reference/prompts/`，团队复制到 Claude Code 使用。

### 工具内部资产（极简版）

```
prd2code-gen/
├── src/reference/
│   ├── spec.md              # 唯一的标准说明文档
│   ├── prompts/              # 8 个通用 prompt 模板（不按层区分）
│   ├── init.ts               # ref-init 命令
│   └── validate.ts           # ref-validate 命令
└── examples/marketing-bff/reference/  # 唯一的完整示例
```

---

## 七、置信度校准（重要发现）

**Nature 2026**：48 个 LLM 置信度与实际准确率严重不匹配，LLM 常对流畅但不正确的输出赋高概率。

| 方法 | 可靠性 | 推荐度 |
|------|--------|--------|
| LLM 自评 | ❌ 低 | 当前方案，需改进 |
| Structured Logprobs | ✅ 高 | 生产推荐 |
| 多次运行一致性 | 高但贵 | 高价值决策 |
| **混合验证（推荐）** | ✅✅ 最高 | 业务规则 + Contract + 回测 |

---

## 八、Reference 新鲜度策略（新增）

### 当前阶段：手动 + 工具辅助

| 层级 | 机制 | 成本 | 覆盖率 |
|------|------|------|--------|
| 被动 | 工作流步骤 2+5 自动发现 drift | 0（自带） | 仅 PRD 涉及部分 |
| 半自动 | 迭代末跑 `ref-validate --lint` | 5 分钟/次 | 全量 |
| 提醒 | metadata 过期提醒 | 前期 1 小时设置 | 100% |

### 长期目标：自动闭环

```
/bff-gen 工作流
  → 步骤 5 发现 reference 与代码不一致
  → 自动提取变更点
  → 输出 drift-report.json
  → 用户确认后自动更新 reference
```

**当前阶段不做**自动更新，先积累 drift-report 数据验证准确率。

---

## 九、实施步骤（精简为 3 Phase）

| Phase | 内容 |
|-------|------|
| **A: 标准+示例+安装器** | spec.md + marketing-bff 迁移到 9 文件 + ref-init |
| **B: Prompt 模板** | 8 个通用 prompt + ref-validate |
| **C: 试点验证** | 营销平台/app/dive 前端试点 + 反馈调整 |

---

## 十、使用方最终产出（新增）

使用方完成 reference 构建后，项目里有三部分产出：

```
their-bff-project/
├── .claude/skills/bff-gen/
│   ├── SKILL.md + workflow.md + steps/    ← 工具部署
│   └── reference/ (9 文件)                 ← 团队构建
├── src/config/template/render/             ← /bff-gen 生成的代码
└── _bff-gen-output/                        ← 运行产出物
    ├── distilled.json + analysis.json + plan.json
    ├── validation.json + report.md
    └── drift-report.json                  ← ⭐ 新增：reference 与代码差异报告
```

---

## 十一、待办事项

- [ ] 后端团队访谈（项目结构、API 契约、数据模型、字段来源边界、已知坑点）
- [ ] 将 marketing-bff reference 迁移到新 9 文件结构
- [ ] 编写 spec.md 标准说明
- [ ] 编写 8 个通用 prompt 模板
- [ ] 更新 installer types.ts 和 deployer.ts

---

## 十、5 个关键结论

1. **工具是流程提供者，不是数据存储** — reference 由各团队维护，工具只定义格式+提供引导+消费执行
2. **8 类知识缺一不可** — Patterns（33%）占比最大，之前只做了 Registry 和 Topology 就以为够了
3. **防偏差 > 生成能力** — 工具的核心价值是强制 AI 按 reference 执行，不是帮 AI 生成更好的代码
4. **PRD 输入质量是最大杠杆** — 投入 PRD 标准化的 ROI 远高于优化蒸馏算法
5. **LLM 自评置信度不可靠** — 需要多源校验（混合验证），不能只靠 LLM 自己打分

---

## 行业参考

| 来源 | 核心洞察 |
|------|---------|
| [Martin Fowler - Knowledge Priming](https://martinfowler.com/articles/reduce-friction-ai/knowledge-priming.html) | AI 代码生成的知识预注入 |
| [3-Tier Knowledge Architecture](https://www.linkedin.com/pulse/copy-we-built-3-tier-knowledge-architecture-ai-coding-shailesh-pant-mmmqc) | 企业级 AI 知识架构 |
| [Nature - LLM Confidence Calibration](https://www.nature.com/articles/s44355-026-00053-3) | LLM 置信度校准不可靠 |
| [structured-logprobs](https://github.com/arena-ai/structured-logprobs) | 字段级置信度评分 |
| [ChatPRD](https://chatprd.ai/learn/prd-for-ai-codegen) | AI 友好 PRD 写法 |
| [GitHub Spec Kit](https://github.com/github/spec-kit) | 规范驱动开发 |
| [Kiro (AWS)](https://kiro.dev/docs/specs/) | 结构化规范 IDE |
| [6 Agentic KB Patterns](https://thenewstack.io/agentic-knowledge-base-patterns/) | 6 种代理式知识库模式 |
| [Virtual Monorepo Pattern](https://medium.com/devops-ai/the-virtual-monorepo-pattern-how-i-gave-claude-code-full-system-context-across-35-repos-43b310c97db8) | 跨仓库知识共享 |

---

*架构文档: `_bmad-output/planning-artifacts/reference-standardization-architecture.md`*
*调研文档: `docs/research/technical-reference-prd-research-2026-04-22.md`*

*维护者: 林泽 (zachary-lz-glm) · AI 助手: bubu 🫧*
